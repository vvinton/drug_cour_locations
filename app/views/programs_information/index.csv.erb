<%=
  CSV.generate do |csv|
    csv << ['State','County','Court Type','Program Name','Address', 'Website']
    @all_results.each do |result|
      csv << [result.state,result.county,result.program_type,result.program_name,result.address,result.website]
    end
  end
%>
